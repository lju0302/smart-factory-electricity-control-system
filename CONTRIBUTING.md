# Contributing

## 변경 단위

코드, 설정, 쿼리, 인프라, 문서는 서로 검증 가능한 단위로 나눕니다. 데이터 계약이나 배포 설정을 바꾸면 영향을 받는 소비자와 운영 문서를 같은 PR에 포함합니다.

## 커밋 접두사

- `feat:` 기능 추가
- `fix:` 오류 수정
- `refactor:` 동작을 유지하는 구조 개선
- `docs:` 문서 변경
- `ci:` GitHub Actions·검증 변경
- `chore:` 의존성·도구·정리 작업

커밋 제목은 현재형으로 짧게 작성하고, 하나의 커밋에서 서로 무관한 변경을 섞지 않습니다.

## 제출 전 확인

- 비밀값·개인 데이터·모델 binary가 포함되지 않았는지 확인
- 변경된 Python 파일의 import와 설정 경로 확인
- SQL·Stream Analytics 쿼리의 입력/출력 이름 확인
- Bicep 변경 시 `az bicep build --file infra/main.bicep` 실행
- README 또는 운영 절차가 바뀌면 관련 문서 링크 확인

## PR 설명

PR에는 다음 내용을 포함합니다.

1. 변경 목적과 영향 범위
2. 변경된 서비스·테이블·이벤트
3. 실행한 검증 명령과 결과
4. 배포 순서, 필요한 secret/variable, 롤백 방법
5. 데이터 계약 또는 운영 정책에 미치는 영향

운영 환경 변경은 what-if와 승인 여부를 PR에 기록합니다.
